# Production Environment Configuration for Athena Web
# Copy to /etc/athena-web/env on ahjo-1

# Node environment
NODE_ENV=production

# Server port (default: 9000)
PORT=9000

# Workspace path
WORKSPACE_PATH=$HOME/.openclaw/workspace

# Beads CLI command
BEADS_CLI=br

# Log level (error, warn, info, debug)
LOG_LEVEL=info

# Optional: Custom state path (defaults to ${WORKSPACE_PATH}/state)
# STATE_PATH=/custom/path/to/state
